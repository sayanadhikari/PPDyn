;
; @file        cubesat_config.ini
; @brief       Configuration file for cubesat geometry and electric field generation
;

[cubesat]
size = 1U              ; Cubesat size: 1U, 2U, 3U, 6U
wing_config = deployed ; Wing configuration: standard, deployed, none
wing_length = 0.15    ; Wing length in meters
include_antenna = True ; Include antenna elements

[domain]
Lx = 0.5              ; Domain size in X (meters)
Ly = 0.5              ; Domain size in Y (meters)
Lz = 0.5              ; Domain size in Z (meters)
nx = 50              ; Grid resolution in X
ny = 50              ; Grid resolution in Y
nz = 50              ; Grid resolution in Z

[electric_field]
floating_potential = 1.0  ; Floating potential in volts
solver_method = sparse    ; Solver method: sparse, fd, fast
fast_mode = False         ; Use fast approximation (good for testing) - set to False for production
use_iterative = True     ; Use iterative solver: True, False, or auto (False = direct solver, more reliable)

[output]
output_dir = PIC_data     ; Output directory for HDF5 files
